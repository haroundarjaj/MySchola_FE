import dashboard from './dashboard';
import views from './views';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, views]
};

export default menuItems;
